import Phaser from 'phaser'; import {Player} from '../entities/Player'; import {LevelManager} from '../systems/LevelManager'; import {LevelFactory,type BuiltLevel} from '../systems/LevelFactory'; import {CollapseSystem} from '../systems/CollapseSystem'; import {EnvironmentSystem} from '../systems/EnvironmentSystem'; import {eventBus,Events} from '../utils/EventBus'; import type {LevelDefinition,LevelSceneData} from '../types/game'; import {StorageService} from '../services/StorageService';
export class LevelScene extends Phaser.Scene{private player!:Player;private level!:LevelDefinition;private built!:BuiltLevel;private collapse!:CollapseSystem;private environment!:EnvironmentSystem;private dead=false;private complete=false;private paused=false;private deaths=0;private levelIndex=0;private totalElapsedMs=0;private lastStart=false;constructor(){super('Level');}
init(data:LevelSceneData):void{this.levelIndex=data.levelIndex??0;this.deaths=data.deaths??0;this.totalElapsedMs=data.totalElapsedMs??0;}
create():void{this.dead=false;this.complete=false;this.paused=false;this.level=new LevelManager().get(this.levelIndex);this.physics.world.setBounds(0,0,this.level.width,this.level.height);this.cameras.main.setBounds(0,0,this.level.width,this.level.height).setBackgroundColor(this.level.backgroundColor);this.environment=new EnvironmentSystem(this,this.level);this.built=new LevelFactory(this).build(this.level);this.player=new Player(this,this.level.spawn.x,this.level.spawn.y);this.collapse=new CollapseSystem(this,this.level.durationMs);this.bindPhysics();this.cameras.main.startFollow(this.player,true,.09,.1).setDeadzone(260,130).fadeIn(240);this.scene.launch('UI');this.input.keyboard?.on('keydown-R',this.restart,this);this.input.keyboard?.on('keydown-ESC',this.togglePause,this);this.events.on('player:dash',this.dashTrail,this);this.events.once(Phaser.Scenes.Events.SHUTDOWN,this.shutdown,this);}
private bindPhysics():void{this.physics.add.collider(this.player,this.built.platforms);for(const moving of this.built.moving)this.physics.add.collider(this.player,moving);for(const falling of this.built.falling)this.physics.add.collider(this.player,falling,()=>falling.trigger());this.physics.add.overlap(this.player,this.built.hazards,()=>this.die());for(const laser of this.built.lasers)this.physics.add.overlap(this.player,laser.hitbox,()=>this.die());for(const timed of this.built.timedZones)this.physics.add.overlap(this.player,timed.zone,()=>this.die());for(const fan of this.built.forceZones)this.physics.add.overlap(this.player,fan.zone,()=>this.player.setVelocity((this.player.body?.velocity.x??0)+fan.forceX*.016,(this.player.body?.velocity.y??0)+fan.forceY*.016));for(const belt of this.built.conveyors)this.physics.add.overlap(this.player,belt.zone,()=>this.player.setVelocityX((this.player.body?.velocity.x??0)+belt.speed*.035));this.physics.add.overlap(this.player,this.built.door,()=>this.finish());}
update(time:number,delta:number):void{const start=Boolean(this.input.gamepad?.getPad(0)?.buttons[9]?.pressed);if(start&&!this.lastStart)this.togglePause();this.lastStart=start;if(this.paused||this.dead||this.complete)return;this.player.update();for(const moving of this.built.moving)moving.update();for(const falling of this.built.falling)falling.update(this.level.height);for(const laser of this.built.lasers)laser.update(time);for(const zone of this.built.timedZones)zone.update(time);this.collapse.update(delta);const urgency=1-this.collapse.timer.remainingMs/this.level.durationMs;this.environment.update(this.cameras.main.scrollX,urgency,time);if(this.player.y>this.level.height+50||this.collapse.timer.expired)this.die();eventBus.emit(Events.HUD,{floor:this.level.floor,totalFloors:5,floorName:this.level.name,remainingMs:this.collapse.timer.remainingMs,durationMs:this.level.durationMs,deaths:this.deaths,dashReady:this.player.dashAvailable,paused:false,progress:this.player.x/this.level.width});}
private die():void{if(this.dead||this.complete)return;this.dead=true;this.deaths+=1;this.player.kill();this.collapse.stop();this.environment.burstSparks(this.player.x,this.player.y,20);const settings=new StorageService().load().settings;if(settings.screenShake)this.cameras.main.shake(140,settings.reducedShake?.002:.006);if(!settings.reduceFlashes)this.cameras.main.flash(80,255,60,80);this.time.delayedCall(90,()=>this.cameras.main.fadeOut(260));this.time.delayedCall(480,()=>this.scene.restart({levelIndex:this.levelIndex,deaths:this.deaths,totalElapsedMs:this.totalElapsedMs}));}
private finish():void{if(this.dead||this.complete)return;this.complete=true;this.collapse.stop();const elapsed=this.level.durationMs-this.collapse.timer.remainingMs;this.cameras.main.zoomTo(1.025,180);this.time.delayedCall(260,()=>{this.scene.stop('UI');this.scene.start('Results',{elapsedMs:elapsed,deaths:this.deaths,floor:this.level.floor,levelIndex:this.levelIndex,totalElapsedMs:this.totalElapsedMs+elapsed,final:this.levelIndex===4});});}
private restart():void{this.scene.restart({levelIndex:this.levelIndex,deaths:this.deaths,totalElapsedMs:this.totalElapsedMs});}private togglePause():void{if(this.dead||this.complete)return;this.paused=!this.paused;if(this.paused)this.physics.world.pause();else this.physics.world.resume();eventBus.emit(Events.HUD,{floor:this.level.floor,totalFloors:5,floorName:this.level.name,remainingMs:this.collapse.timer.remainingMs,durationMs:this.level.durationMs,deaths:this.deaths,dashReady:this.player.dashAvailable,paused:this.paused,progress:this.player.x/this.level.width});}
private dashTrail(x:number,y:number):void{for(let i=0;i<4;i+=1){const ghost=this.add.image(x-i*9,y,'player-dash').setAlpha(.45-i*.08);this.tweens.add({targets:ghost,alpha:0,duration:180+i*20,onComplete:()=>ghost.destroy()});}}
private shutdown():void{this.input.keyboard?.off('keydown-R',this.restart,this);this.input.keyboard?.off('keydown-ESC',this.togglePause,this);this.events.off('player:dash',this.dashTrail,this);this.environment.destroy();this.scene.stop('UI');}}
