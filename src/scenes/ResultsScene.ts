import Phaser from 'phaser';import type {ResultData} from '../types/game';import {LEVELS} from '../config/levelConfig';import {StorageService} from '../services/StorageService';import {calculateRank,seconds} from '../systems/Statistics';
export class ResultsScene extends Phaser.Scene{constructor(){super('Results');}create(data:ResultData):void{const level=LEVELS[data.levelIndex];if(!level)throw new Error('Invalid result level');const rank=calculateRank(level,data.elapsedMs,data.deaths);new StorageService().recordFloor(data.floor,data.elapsedMs,data.deaths,rank);this.cameras.main.setBackgroundColor('#0c1119');this.add.text(480,105,data.final?'EVACUACIÓN COMPLETA':'PISO COMPLETADO',{fontFamily:'monospace',fontSize:'42px',color:'#5ef1ff',fontStyle:'bold'}).setOrigin(.5);this.add.text(480,205,`RANGO  ${rank}\n${level.name}\nTIEMPO ${seconds(data.elapsedMs)} s · MUERTES ${data.deaths}${data.final?`\n\nTOTAL ${seconds(data.totalElapsedMs)} s`:''}`,{fontFamily:'monospace',fontSize:'22px',color:'#fff',align:'center',lineSpacing:10}).setOrigin(.5);this.add.text(480,420,data.final?'ENTER · MENÚ':'ENTER / A · SIGUIENTE PISO',{fontFamily:'monospace',fontSize:'18px',color:'#f5c84c'}).setOrigin(.5);const next=()=>data.final?this.scene.start('Menu'):this.scene.start('Level',{levelIndex:data.levelIndex+1,deaths:0,totalElapsedMs:data.totalElapsedMs});this.input.keyboard?.once('keydown-ENTER',next);this.input.gamepad?.once('down',next);}}
